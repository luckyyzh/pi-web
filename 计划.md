# SSH 远程化改造计划（单目录模式 + 影子目录）

> 目标：远程模式下，左侧路径选择器可切换远程目录，会话按远程目录独立分组（切回保留），
> 文件浏览器跟随，agent 工具（read/write/edit/bash）转发远程，git/AGENTS/项目信任远程化。
>
> 核心方案：**影子目录**——每个远程目录映射一个本地影子根（`~/.pi/remote/<host>_<hash>/`），
> 会话/信任/AGENTS 用影子根，文件/agent 操作通过「影子路径 → 远程路径」映射走 ssh。
> 同一远程目录永远同一影子根（确定性 hash）→ 不破坏 prompt 前缀缓存。
>
> 前置条件：SSH 密钥免密；远程有 bash/tar/base64。

---

## 阶段① 影子目录 + 会话隔离 + 文件浏览 + AGENTS/信任

### 1. 影子目录基础设施（`lib/ssh.ts`）
- [x] 影子根映射：`shadowRootFor` / `ensureShadowRoot` / `activeShadowRoot`（确定性 hash，目录真实创建）
- [x] 路径双向映射：`localToRemotePath`（影子路径 → 远程路径）
- [x] 远程目录浏览/校验：`sshBrowse` / `sshPathExistsDir`
- [x] 远程文件操作：`sshListDir` / `sshReadTextFile` / `sshExec` / `sshTestConnection`
- [x] 影子同步：`syncShadowProject`（远程 AGENTS.md / .pi / .agents → 影子根，幂等）
- [x] tsc 校验通过

### 2. 后端 API 适配
- [x] `/api/cwd/validate`：远程路径校验（ssh 检查存在）→ 返回影子根作为规范化 cwd
- [x] `/api/cwd/browse`：远程目录浏览（ssh ls，支持上下级导航）
- [x] `/api/agent/new`：远程模式放行影子根 cwd + 触发影子同步（validate 已同步）
- [x] `/api/files/[...path]`：远程分支改用 `localToRemotePath`（影子映射）
- [x] `/api/ssh/config`：保存时自动创建影子根 + 同步，GET 返回 shadowRoot

### 3. 前端
- [x] `SessionSidebar`：远程模式下顶部路径显示远程路径（影子根 → 远程翻译）
- [x] `DirectoryPicker`：远程模式浏览/选择远程目录（ssh 导航）
- [x] `AppShell`：远程模式状态传递 + 切换目录后刷新会话/文件树

### 4. ssh 扩展（`~/.pi/agent/ssh/extensions/ssh.ts`）
- [x] `toRemote` 映射基准改为「影子根 → 远程根」（读 ssh-config）
- [x] 路径翻译逻辑与 pi-web 后端保持一致

---

## 阶段② git 远程化

### 5. git 远程化
- [x] `lib/git-changes.ts`：git 命令走 ssh（`ssh host "cd <远程> && git ..."`），文件内容走 ssh
- [x] `lib/git-status.ts` / `/api/git/status`、`/api/git/diff`：远程分支
- [x] 远程 git 仓库判定（`git rev-parse --show-toplevel` 走 ssh）

---

## 验证与交付

- [x] `npx tsc --noEmit` 全量通过
- [x] `npm run build` 通过（新路由注册）

## 实测修复（用户实测后发现的 bug）

- [x] 启用远程后自动切 cwd 到影子根（修复文件浏览器 Access denied + 目录不切换）
  - `components/SessionSidebar.tsx`：监听 sshInfo，启用远程自动 `setSelectedCwd(影子根)`、退出切回本地
  - `components/AppShell.tsx`：onModeChange 仅刷新（cwd 切换移交 SessionSidebar）
  - 根因：AppShell `handleCwdChange` 后 selectedSession/newSessionCwd 均空 → selectedCwdProp=null → 文件树不同步；改为 SessionSidebar 内部 setSelectedCwd
- [x] 目录选择器远程目录名不显示（只显示图标）修复
  - 根因：`/api/cwd/browse` 远程分支返回 `dirs` 为 string[]，而 DirectoryPicker 期望 `{ name, path }[]` → `entry.name` 为 undefined
  - `app/api/cwd/browse/route.ts`：远程分支把 dirs 映射为 `{ name, path }` 与本地统一
- [x] 影子根加入 allowed roots（修复 Access denied 根因：`getAllowedFileRoots` 不含影子根 → list 在权限检查即被拒，远程分支未执行）
  - `lib/file-access.ts`：远程模式激活时把影子根加进 allowed roots，全链路生效
- [x] 用户重启 pi-web（next start）加载新产物
- [x] 实测：切换远程目录 / 独立会话 / 文件浏览器 / agent 工具 / git / 缓存命中率
- [x] 更新 README / 使用说明

## 一键安装 ssh 扩展 + 文档

- [x] `start-pi-web.cmd` 一键启动（自动装 pi agent / 依赖 / 构建 / 端口检测）
- [x] ssh 扩展包内嵌 `pi-web/vendor/ssh`
- [x] `/api/ssh/install-extension`：GET 检测已安装 + POST 一键安装（复用 packageManager.installAndPersist）
- [x] `SshConfig` 弹窗：检测到 ssh 扩展未安装时显示「一键安装 ssh 扩展」按钮
- [x] README 加「Remote SSH Workspace」章节（含依赖 ssh 扩展说明）
- [x] 重启验证一键安装按钮（已随卸载修复重启验证）
- [x] 修复本地包卸载失败：pi 卸载匹配 key 用 process.cwd() 解析本地包相对路径，webUI 下 cwd≠agent baseDir → key 不一致 → 不移除；npm 包 key 与 cwd 无关故能卸载。
  - `app/api/plugins/route.ts`：仅本地包先 `getInstalledPath` 转绝对路径再 remove（npm/git 包保持原 source）
  - 已用脚本验证 5 个包全部 match，tsc/build 通过，已推送 8eefd7a，用户实测通过

---

## 备注 / 决策记录

- 影子根 hash = `sha256(host + path)` 前 12 位 → 同一远程目录映射稳定，**不破坏前缀缓存**
- AGENTS.md / .pi / .agents 通过 `tar` 从远程同步到影子根（幂等，内容不变则文件不变）
- 项目信任基于影子根的 `.pi/.agents` 自动生效（无需改 pi 核心）
- 切换目录 = 换影子根 = 换会话（预期行为，缓存前缀短暂重建后回升）
- 不做「每目录独立 api-key」（收益甚微，不值得复杂度）
